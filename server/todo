Move Team signup to registration page, after the first page has been submitted.
Setup error logging

only show eligible teams on scoreboard (all teams on admin scoreboard)
    -teams with no admin or banned members

Style everything

admin function that backs up the database


Add the other pages (chat, learn, faq, news, teachers)

make loadProblem script preserve problem id if the name is the same (allows for adding problems during competition)

decide if links on admin page should be for /admin/* or /* (in which case /admin links should be displayed elsewhere on the page)
